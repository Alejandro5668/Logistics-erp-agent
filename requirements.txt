sqlalchemy>=2.0
langchain-core>=0.1
langchain>=1.4,<2
langgraph>=1.2,<2
pytest>=7.0
chromadb==0.5.4  # pinned exact: newer 0.5.x releases have no prebuilt chroma-hnswlib wheel for Windows/cp312, forcing an MSVC build
fastapi>=0.141,<1  # verified installed: 0.141.1, compatible with pydantic 2.13.5 already required by langchain 1.4.0
uvicorn>=0.52,<1  # verified installed: 0.52.4
httpx>=0.28,<1  # verified installed: 0.28.1; TestClient dependency for PR 2's integration suite
